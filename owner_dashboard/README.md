# Owner Dashboard

Not started yet. This will be the owner-facing website for:

- Creating projects with location + timeline
- Assigning available workers to a project/site and notifying them
- Viewing per-worker, per-site clock-in/out history and progress photos
- A simple overall dashboard of project progress and worker status

It will read from the same Supabase project as `employee_app` — see `../supabase/migrations`.
